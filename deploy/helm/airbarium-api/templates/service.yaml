apiVersion: v1
kind: Service
metadata:
  name: {{ include "airbarium-api.fullname" . }}
  labels:
    {{- include "airbarium-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "airbarium-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
